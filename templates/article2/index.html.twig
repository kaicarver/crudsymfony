{% extends 'base.html.twig' %}

{% block title %}Article2 index{% endblock %}

{% block body %}
    <h1>Article2 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article2 in article2s %}
            <tr>
                <td>{{ article2.id }}</td>
                <td>{{ article2.titre }}</td>
                <td>{{ article2.description }}</td>
                <td>
                    <a href="{{ path('app_article2_show', {'id': article2.id}) }}">show</a>
                    <a href="{{ path('app_article2_edit', {'id': article2.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_article2_new') }}">Create new</a>
{% endblock %}
