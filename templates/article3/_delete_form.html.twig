<form method="post" action="{{ path('app_article3_delete', {'id': article3.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article3.id) }}">
    <button class="btn">Delete</button>
</form>
